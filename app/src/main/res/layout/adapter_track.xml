<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="track" type="com.darekbx.geotracker.model.Track" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/track_id"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:padding="@dimen/padding_medium"
            android:text="@{track.id + ``}"
            />
        <TextView
            android:id="@+id/track_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/track_id"
            android:paddingTop="@dimen/padding_medium"
            android:text="@{track.label == null ? `[empty]` : track.label}"
            android:textColor="@android:color/black"
            />
        <TextView
            android:id="@+id/track_start_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:padding="@dimen/padding_medium"
            android:text="@{track.startTimestamp + ``}"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>