<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="android.view.View" />
        <variable name="yearSummary" type="com.darekbx.geotracker.model.YearSummary" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/row_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="@dimen/padding_small"
        android:paddingTop="8dp">
        <TextView
            android:id="@+id/year_distance"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:paddingStart="52dp"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:textSize="18sp"
            android:text="@{@string/distance_format(yearSummary.totalDistance+200)}"
            tools:text="10.56km"
            />
        <TextView
            android:id="@+id/year_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/year_distance"
            android:paddingStart="@dimen/padding_small"
            android:paddingEnd="@dimen/padding_medium"
            android:text="@{`(` + yearSummary.totalCount + `)`}"
            android:textSize="18sp"
            tools:text="(50)"
            />
        <TextView
            android:id="@+id/year_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:paddingEnd="@dimen/padding_medium"
            android:text="@{yearSummary.year}"
            android:textSize="18sp"
            tools:text="2020"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
